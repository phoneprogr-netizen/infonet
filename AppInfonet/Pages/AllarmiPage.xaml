<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="AppInfonet.Pages.AllarmiPage"
    Title="Allarmi"
    BackgroundColor="#F1F4F9">

    <Grid RowDefinitions="Auto,*" Padding="16">

        <Button
            Grid.Row="0"
            Text="Storico allarmi"
            HorizontalOptions="Fill"
            HorizontalTextAlignment="Center"
            BackgroundColor="White"
            TextColor="Black"
            CornerRadius="22"
            Clicked="OnStoricoClicked" />

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <CollectionView
                ItemsSource="{Binding Allarmi}"
                SelectionMode="None">

                <CollectionView.EmptyView>
                    <Label Text="Nessun allarme disponibile"
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="0,6"
                               Padding="12"
                               CornerRadius="12"
                               HasShadow="True"
                               BackgroundColor="White"
                               BorderColor="Black">
                            <VerticalStackLayout Spacing="8">
                                <Label Text="{Binding MessaggioDisplay}" FontAttributes="Bold" FontSize="16" TextColor="Black" />

                                <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="12">
                                    <Label Grid.Column="0" Text="{Binding DataOraDisplay}" FontSize="13" TextColor="Black" />
                                    <Label Grid.Column="1" Text="Stato" FontSize="13" TextColor="Black" />
                                    <Label Grid.Column="2" Text="{Binding StatoDisplay}" FontSize="13" FontAttributes="Bold" TextColor="Black" />
                                </Grid>

                                <Button Text="Apri su mappa"
                                        BackgroundColor="White"
                                        TextColor="Black"
                                        CornerRadius="20"
                                        Clicked="OnApriMappaClicked"
                                        CommandParameter="{Binding IdTestataAllarme}" />

                                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
                                    <Button Grid.Column="0"
                                            Text="✕"
                                            BackgroundColor="White"
                                            TextColor="Black"
                                            CornerRadius="22"
                                            Clicked="OnCloseEventClicked"
                                            CommandParameter="{Binding IdTestataAllarme}" />
                                    <Button Grid.Column="1"
                                            Text="🔕"
                                            BackgroundColor="White"
                                            TextColor="Black"
                                            CornerRadius="22"
                                            Clicked="OnSnoozeEventClicked"
                                            CommandParameter="{Binding IdTestataAllarme}" />
                                    <Button Grid.Column="2"
                                            Text="🎧"
                                            BackgroundColor="White"
                                            TextColor="Black"
                                            CornerRadius="22"
                                            Clicked="OnSendToOperationCenterClicked"
                                            CommandParameter="{Binding IdTestataAllarme}" />
                                </Grid>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Border
                StrokeThickness="0"
                BackgroundColor="#80FFFFFF"
                IsVisible="{Binding IsLoading}">
                <VerticalStackLayout
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    Spacing="8">
                    <ActivityIndicator
                        IsRunning="{Binding IsLoading}"
                        IsVisible="{Binding IsLoading}" />
                    <Label Text="Caricamento allarmi in corso..."
                           FontSize="14"
                           HorizontalTextAlignment="Center" />
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
